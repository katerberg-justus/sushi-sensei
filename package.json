{
  "name": "sushi-sensei",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader native --host 127.0.0.1 --port 5174 --strictPort",
    "build": "vite build --configLoader native",
    "preview": "vite preview --configLoader native --host 127.0.0.1"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "vite": "^6.0.0"
  },
  "devDependencies": {}
}
